# SDD ledger — plan: /mnt/d/Documents/Code/GitHub/ImageAI/Plans/2026-08-29-sprite-image-route-exports-plan.md
Chain position: 6 of (1 ✓ → 2 ✓ → 3 ✓ → 4 ✓ → 5a (running) → 5b → 6).
Spec: Plans/2026-08-29-sprite-tab-design.md §2, §3 row 6, §4.6, §5 (reachable).
Pre-flight scan: ~55 rows in preflight-scan.md; 0 MISMATCH/CONFLICT (two stale core/utils.py line citations — cosmetic). Every 5a/5b seam matches the sibling plan text verbatim. slice_sheet out_dir hazard never triggered (sources under clips/, out_dir = stages/<id>/extract).
Chain position update (2026-08-30): 1 ✓ → 2 ✓ → 3 ✓ → 4 ✓ → 5a ✓ → 5b ✓ (45f9796..9898946, suite 1775) → 6 (this).
Carry-forward from 5b final review (deferred to 6 — triage when the owning task is dispatched):
- Minor 4: modal helper dialogs parented to long-lived widgets are never deleted after exec().
- Minor 5: a superseded probe orphan keeps ProcessingPanel.is_busy() True until the old ffprobe returns (Run briefly gated).
- Minor 6 + T7: recycle-bin failure during purge-after-export is reported as success, and the purge runs on the GUI thread — route a non-empty failure list through QMessageBox.warning and move the purge into the export worker.
- Minor 10: PixelView decode failures logged twice on the workspace path.
- T7 note for 7: _grid_output_paths duplicates grid.py naming — export_grid should return the paths it wrote.
- FrameOverridesDialog refusal shows a box but no console line (no logMessage signal on the dialog).
- 5a carry-forwards still open: CharacterPanel Ctrl+Enter (deviation 16), queue console log levels, GUI-only-writer save redesign, positional current-tab persistence, getsource wiring tests, analyze_source on the GUI thread, _on_send_to_sprite TypeError show_error.
Environment facts for 6: WorkerHost binds WEAK references into signal partials and disconnects after release; shutdown()->bool + orphans (_LIVE_ORPHANS) + join_orphans() + _on_worker_idle() hook — any new WorkerHost subclass must override _on_worker_idle and never write self._worker. ExportDialog seam ships as specified (register_format applies persisted formats to late registrations; out_dir persisted per project keyed by project_dir.name). Shortcuts are owner-scoped (SHORTCUT_WIDGET_ATTRS); SpriteTab.save_current_project() is the public autosave; FramesWorkspace.apply_frames(action_id, frames, label) is the retouch edit path (new list, no own snapshot). tests/sprite/gui/conftest.py has an autouse teardown gc.collect(); scratch files never in the repo.
Task 1: dispatched (impl-ir-1, sonnet) at BASE e83e39c
Task 3: dispatched (impl-ir-3, sonnet) in parallel with Task 1 at BASE e83e39c — disjoint files (godot_tres.py vs aseprite_native.py); Task 2 waits on Task 1's symbols
Task 1: implemented a6ea74c; review dispatched (rev-ir-1) — package review-e83e39c..a6ea74c.diff
Task 2: dispatched (impl-ir-2, sonnet) at BASE a6ea74c — consumes Task 1 symbols; disjoint from Task 3 (in flight)
Task 3: implemented 80d6209; review dispatched (rev-ir-3) — package review-a6ea74c..80d6209.diff; note: brief's own test helper emits a Pillow mode-param DeprecationWarning (pristine rule — reviewer to judge)
Task 5: dispatched (impl-ir-5, sonnet) at BASE 80d6209, parallel with 2 (Task 8 waits on Task 6/7 helpers)
Task 1: Ruling: plan-mandated Important (no escaping of .tres-embedded strings — tag.name / atlas_res_path with a quote or backslash breaks the resource; brief's own prototype) ruled FIX — minimal escaping (" → \", \ → \\) for both fields + one test. Cost if wrong: ~6 lines.
Task 1: minor (deferred): golden comparison is whitespace-normalized (brief's own test shape).
Task 1: fix round 1/5 dispatched to impl-ir-1.
Task 3: Ruling: plan-mandated Important (28 Pillow mode-param DeprecationWarnings from the brief's own test helper) fixed by the controller (de64183): Image.fromarray(arr) infers RGBA — reviewer verified zero output change. Minor (deferred): tags chunk writes from/to unclamped (matches aseprite_json convention).
Task 3: complete (commits 80d6209 + controller warning fix, review clean)
Task 1: fix round 1/5 (escaping — 142839b; verified by the controller against the diff: _escape at both sites + discriminating test; 12 tests pristine). Task 1: complete (commits a6ea74c..142839b, review clean)
Task 2: implemented 3115774 (2 brief-bug deviations: grid extrude/border validation constants; fps_reconciliation drift check redesigned — was a mathematical no-op); review dispatched (rev-ir-2, opus)
Task 5: implemented 2892732; review dispatched (rev-ir-5) — implementer noted 34 warnings in its full-suite run (likely pre-de64183 Pillow ones; reviewer to count on its own run)
Task 5: Ruling: plan-mandated Important (pose_steps bypasses _common.emit → full request/response logged twice on the default log path; every sibling module uses emit; the brief's Consumes list omitted it) ruled FIX — route all log sites through emit(logger, log, message). Cost if wrong: ~6 lines.
Task 5: fix round 1/5 dispatched to impl-ir-5.
Task 2: review — spec compliant; both deviations verified as real brief bugs (grid validation constants; fps drift gate unreachable — re-derived from timing.py:90). Ruling: Important (export manifest breaks after the first sidecar candidate → <title>.png.json never returned; Task 4/7 consume the list as the manifest) FIX — append every existing candidate, and cover @Nx sheets+sidecars if scales are set. Minors deferred: drift note lacks a magnitude gate (brief's verbatim test pins the shape — plan-owner note); degenerate-duration text; repeat==1 unnoted.
Task 2: fix round 1/5 dispatched to impl-ir-2.
Task 6: dispatched (impl-ir-6, sonnet) at BASE 2892732, parallel with the Task 2/5 fix rounds (disjoint files; pose_steps signature stable through its logging fix)
Task 5: fix round 1/5 (emit routing — 66c7c88; verified by the controller against the diff: 4 sites through emit with levels, once-only test added; 14 tests pristine). Task 5: complete (commits 2892732..66c7c88, review clean)
Task 2: fix round 1/5 (manifest — d50cba9; verified by the controller: _grid_output_paths lists sheet+both sidecars per scale unconditionally, matching export_grid's unconditional writes, pinned by a recursive-disk-listing test). Pillow mode-arg warning in its test helper fixed by the controller (34a935e).
Task 2: complete (commits 3115774..34a935e, review clean)
Task 4: dispatched (impl-ir-4, sonnet) at BASE 34a935e — Tasks 2+3 closed; touches gui/sprite/export_dialog.py (no concurrent owner)
Task 6: implemented 66233b8 (brief test-count typo noted); review dispatched (rev-ir-6) — package review-34a935e..66233b8.diff
Task 7: dispatched (impl-ir-7, sonnet) at BASE 66233b8 — consumes Task 6 helpers; disjoint from Task 4 (in flight)
Task 7: CORRECTION — held, not dispatched: Task 7 appends to Task 6's two files, and a Task 6 fix round would collide; dispatch after the Task 6 review verdict.
Task 6: warning fix a3ec539 (test-only, fromarray mode arg; 11 passed 0 warnings) landed after the review package was cut — reviewer notified.
Task 6: Ruling: plan-mandated Important (log_request/log_response bypass _common.emit → double-log on the default path, same class as Task 5's) + Minor (call_provider error path calls logger.error only, never the log sink) ruled FIX together — route through emit(logger, log, …, level=…) like pose_steps/plate/video_route. Minor (deferred): default_openai_edit_model/openai_edit_size unused until Task 8 (brief's own forward helpers). Cost if wrong: ~10 lines.
Task 6: fix round 1/5 dispatched to impl-ir-6; Task 7 remains held until it lands.
Task 6: fix round 1/5 (emit routing + error-path sink — a97e969; 27 tests 0 warnings). Verified by the controller against the diff. Task 6: complete (commits 66233b8..a97e969, review clean)
Task 7: dispatched (impl-ir-7, sonnet) at BASE a97e969 — appends edit_chain to image_route.py (file now stable)
Task 4: implemented bc0de04 (deviations: fromarray fix; 3 pre-existing 5b export-dialog tests re-idd from 'godot_tres' to 'custom_fmt' — real-registration collision); review dispatched (rev-ir-4)
Task 4: Ruling: plan-mandated Important (write_godot_tres/write_aseprite_native return [primary] while their exporters also write .json sidecars — third instance of the manifest class) ruled FIX: include the sidecar paths in the returned lists; tests assert list == disk. Ruling: the dropped notes_label assertion in the 5b test (reviewer reproduced it still passing) must be RESTORED — the removal was outside the authorized scope. Minor (deferred): unused SimpleNamespace import (brief artifact).
Task 4: fix round 1/5 dispatched to impl-ir-4.
Task 4: fix round 1/5 (38bb73e — writers return primary + sidecar; notes_label assertion restored; verified by the controller against the diff; 37 gate tests pristine). Task 4: complete (commits bc0de04..38bb73e, review clean)
Task 7: implemented e7d3b16 (3 deviations: log= threading; openai_edit_size round-half-up fix; dropped an untestable spec-mock assertion); review dispatched (rev-ir-7)
Task 8: dispatched (impl-ir-8, sonnet) at BASE e7d3b16 — new files retouch.py/test_retouch.py, disjoint from a possible Task 7 fix round
Task 7: Ruling: Important 1 (matte plates NNNN.white/black.png written with no sidecars — repo hard rule; brief's own prototype) + Important 2 (session-start-failure log line bypasses emit) + the provenance Minor (matte-mode sidecar cites the merged frame as reference while the chain feeds the white plate) all ruled FIX in one round. Deviations (rounding fix, dropped spec-mock assertion, log threading) accepted — reviewer re-derived each. Cost if wrong: ~12 lines.
Task 7: fix round 1/5 dispatched to impl-ir-7.
Task 8: implemented 22618af (deviations: test-fixture region overlap fix; fromarray; emit/log threading per post-brief file state); review dispatched (rev-ir-8)
Task 7: fix round 1/5 (b2a76c2 — plate sidecars + emit-routed session warning + honest matte provenance; 24 gate tests pristine; verified by the controller against the diff). Task 7: complete (commits e7d3b16..b2a76c2, review clean)
Task 7: minor (deferred to final review): bare single-sink log() progress lines remain in the edit_chain main loop and generate_sheet/slice_generated_sheet (no double-log — only the emit raising-sink safety net is missing).
Task 8: minor (deferred): unused List import; nonexistent neighbor paths silently dropped without a log line.
Task 8: complete (commit 22618af, review clean)
Task 9: dispatched (impl-ir-9, sonnet) at BASE b2a76c2 — Task 8 helpers stable
Task 9: implemented 33050d1 (deviations: apply_retouch defers to apply_frames' internal autosave; fromarray; brief off-by-one test count); review dispatched (rev-ir-9)
Task 10: dispatched (impl-ir-10, opus) at BASE 33050d1 — sprite_tab.py free again
Task 9: Ruling: plan-mandated Important (on_dialog_close cancels + waits 2 s then drops the worker — a still-running QThread parented to the closing dialog aborts the process; brief's own prototype; plus retouch_frame never polls the token inside its retry loop, so the 2 s bound is unreachable in practice; no close-while-busy test) ruled FIX: close path mirrors ExportDialog (WorkerHost-style bounded shutdown + orphan adoption/unbounded fallback, or refuse like ml_install — implementer picks per UX and says which); retouch_frame gains an optional token kwarg checked before each attempt and after each provider call; add the close-while-busy test with a real gated worker. Cost if wrong: ~25 lines across two files.
Task 9: fix round 1/5 dispatched to impl-ir-9.
Task 10: implemented 5c6bc95 (resumed after an API server error); review dispatched (rev-ir-10, opus). Pre-ruled FIX for its round: the close-while-busy path copies retouch_dialog's old cancel+wait(2000)+clear shape — must mirror Task 9's fixed shape once that lands. The test_retouch failure it saw is Task 9's in-flight WIP, not Task 10's.
Task 10: review at task-10-review.md — 0 Critical, 5 Important, 9 Minor; 27 tests clean. Ruling: ALL 5 Important FIX in one round — (1) close path never drops a running worker (use the committed export_dialog/WorkerHost shape; independent of Task 9's timing); (2) _make_pose_fn maps gemini→google for get_api_key/get_auth_mode (the 5a lesson — reuse/mirror action_cards_panel's _config_key_for) and the test's fake config must only answer the mapped key; (3) snapshot combo/widget values on the GUI thread before building the job; (4) failure/cancel path restores or never swaps action.frames, keeps an honest status/error, and saves; (5) record_actual writes a ledger row for paid steps even on partial failure. Minors to the final-review triage.
Task 10: fix round 1/5 dispatched to impl-ir-10.
Task 9: fix round 1/5 (84f0bff — RetouchDialog mixes in WorkerHost, bounded shutdown + join_orphans on close; retouch_frame token kwarg; discriminating tests verified by reversion; 42+3 gate green). Task 9: complete (commits 33050d1..84f0bff, review clean)
Task 10: fix round 1/5 (4567f8a — all 5 rulings + archive-collision Minor; key-mapping test reversion-verified); re-review dispatched (rev-ir-10) — package review-84f0bff..4567f8a.diff
Task 10: re-review at task-10-rereview.md — all 5 Important CLOSED, 0 open; 4 of 9 Minors incidentally resolved; 5 open + 4 new Minors → final-review triage: M1/M2/M6/M7/M9 (original) plus (a) billed_units under-counts matte steps, (b) archive collision loop untested, (c) cancel_render is a no-op against an orphaned worker, (d) failure path discards the previous status. 42 tests, 0 warnings.
Task 10: complete (commits 5c6bc95..4567f8a, re-review Approved)
NEXT: Task 11 (full suite + guard greps + plan bookkeeping), then the sub-project 6 final-review workflow → ONE fix wave → ONE scoped re-review → close out.
Task 11: Step 1 full suite on 4567f8a — 1887 passed, 19 skipped, 2 warnings in 228.87s (0:03:48) (controller-run 2026-08-30 18:35). Guard greps + plan ticks still pending; plan close-out commit deferred until after the final-review fix wave.
Task 11: Step 2 guard greps CLEAN (controller-run 2026-08-30 18:47) — the only model-ID hits in the six new runtime modules are the two permitted `MODEL_CAPS["gpt-image-1"]` fallback keys at image_route.py:100 and :115, which mirror providers/openai.py `_caps_for`. retouch.py, pose_steps.py, image_route_dialog.py, retouch_dialog.py: zero hits.
Final review: RANGE CORRECTION — the handoff named `45f9796..4567f8a` (44 commits); that base is 5a's close and spans all of 5b, which already had its own final review + re-review. Sub-project 6's real base is `e83e39c` (5b close, = plan-6 Task 1 BASE). Package cut as `review-e83e39c..4567f8a.diff` — 21 commits, 24 files, +3946/-9.
Final review: context written (final-review-context.md, 187 lines) and dynamic Workflow launched (run wf_4704bf0c-845, task wa3xr6b7g): 6 dimension reviewers (spec, providers, exporters, threading, seam, tests) + deferred-minor triage → 3-lens adversarial verify (correctness / spec / reproduce, majority survives) → synthesize to image-route-final-review.md.
Final review COMPLETE (image-route-final-review.md, 537 lines; workflow wf_4704bf0c-845: 74 agents, 0 errors, 6.7M subagent tokens, 28.4 min). Verdict **Needs fixes**: 0 Critical, 8 Important, 6 Minor, 8 findings REFUTED by the 3-lens panel (incl. the aseprite/aseprite_json name collision, the sheet-route ledger unit claim, and the retouch-docstring withdrawal — each shown to need code that does not exist).
Controller rulings on the final review — ONE fix wave, 4 parallel implementers on disjoint file sets, no commits by them (controller commits by path):
- ALL 8 Important ruled FIX. Important 6 is the most consequential and was never caught because run_pipeline is a MagicMock in the GUI test: edit_chain writes NNNN.white/black.png into the extract stage dir, pipeline.list_frames is an unfiltered glob("*.png"), so a 3-frame matte render yields 9 action.frames ordered 0001.black/0001/0001.white — plates shown as frames, keying+stabilize paid 3x. Fix is core-side only (plates -> out_dir/"plates"); list_frames is a sub-project 1 contract and must NOT be filtered.
- Important 7 + 8 (missing busy guards on the two new card-row entry points) ruled FIX against the shipped 5b shape at frames_workspace.py:322-327; Important 8 also gets a bounds re-check in apply_retouch so a stale index cannot raise out of a Qt slot.
- M9 ruled FIX-NOW per the triage (undo snapshot for a non-current action; global constraint 12 requires apply_frames).
- Minors 1, 2, 4, 5, 6 ruled FIX (cheap, same files already open; Minor 1 = recurring class 2, ruled Important twice before).
- Minor 3 (deleteLater on the two new dialogs) ruled DEFER to 7 with the triage: DialogCleanupMixin deletes nothing by design and 5b's ExportDialog shares the pattern, so fixing only the two new dialogs diverges from the family. Sub-project 7 owns one consistent pass.
- Every other triage row ruled defer/drop as the table records; carry the defer rows into the sub-project 7 brief, plus the note that export_with_preset needs a unique out_dir per profile per preset.
Fix wave dispatched: A = engine_presets + its tests + test_godot_tres (Imp 1, 2; Minor 4); B = pose_steps + tests (Imp 3); C = image_route + retouch + their tests (Imp 4, 5, 6; Minors 1, 2) [opus]; D = image_route_dialog + retouch_wiring + their GUI tests (Imp 7, 8; M9; Minors 5, 6) [opus]. Every implementer must reversion-check each new test.
Fix wave A: DONE, verified by the controller against the diff, committed by path as a9703b5. Important 1 (png_sequence sidecars in the manifest) + Important 2 (GIF sidecar frame count via ordered_frame_indices) + Minor 4 (golden .tres compared line by line). Implementer improvement accepted: export_gif already writes its own sidecar, so the preset fields MERGE into it (read_image_sidecar + update) instead of overwriting — durations_ms/loop/warnings/timestamp survive, which the brief's suggested fix would have destroyed. Both new tests reversion-checked; 24 passed, 0 warnings; golden needed no rewrite (it already matched line for line).
Fix wave B: DONE, verified by the controller against the diff, committed by path as 0a655b4. Important 3 (pose-step default model resolved to the literal string "chat"): now calls action_cards.default_chat_model(provider), which normalizes the alias, picks the per-provider registry family and carries an offline static default. No import cycle (action_cards does not import pose_steps); generate_pose_instructions signature unchanged so image_route's re-export is intact; no model-ID literal enters pose_steps.py. Controller check: resolve_model is offline-safe (bundled snapshot + static_default), so the new unmocked test cannot go flaky on the network. Reversion-checked; 12 passed, tests/sprite 812 passed, 0 new warnings.
Fix wave C: DONE_WITH_CONCERNS, verified by the controller against the diff, committed by path as d51c164. Important 6 (plates -> out_dir/plates so list_frames sees only composed frames; list_frames itself untouched), Important 4 (prompt/params/sidecar derive from sent_neighbors — empty on the Gemini region branch, because GoogleProvider.edit_image_region at providers/google.py:1907 takes exactly one image), Important 5 (logger kwarg on log_request/log_response/call_provider, defaulted to image_route's logger so existing callers are unaffected; retouch passes its own), Minor 1 (4 bare log() lines routed through emit), Minor 2 (token polled at the top of the plate loop). Controller check: save_png already does mkdir(parents=True, exist_ok=True), so the new plates dir is created. 4 new tests + 1 control, each reversion-checked; 51 passed, 0 warnings.
Controller RULING on C's concern 1 — Minor 2 stays ONE poll, not two. C's argument is better than the brief's: a second poll after log_response fires only on a step's LAST plate, where it saves no provider call and instead discards a frame the user already paid for, drops that frame from the on-disk count billed_units reads, and breaks the existing test_edit_chain_cancels_between_steps. Polling at the top of the plate loop is what actually prevents buying the NEXT call. Accepted as implemented.
Controller action on C's concern 2 — the now-stale billed_units docstring at image_route_dialog.py:56-63 (it credits the plates' non-numeric stem for excluding them, but after C's move the non-recursive glob never sees them) belongs to fix-ir-D's file. Handed to D by message while D was still in flight; isdigit() guard stays as belt-and-braces.
Fix wave D: DONE after TWO controller send-backs, verified by the controller against the file (not the report), committed by path as dd48719. Important 7 + 8 (busy guards mirroring frames_workspace.open_export_dialog; apply_retouch re-validates the index so a stale index cannot raise out of a Qt slot), M9 (every card takes the undo path), Minors 5 + 6 (3 billed_units tests + frozen-clock archive-collision test).
  Send-back 1: D's own concern — its M9 change made EVERY render take the restore-then-apply path, which exposed a discard. apply_frames returns early on an action id the open project does not hold (frames_workspace.py:283-287), and the pre-render restore had already run, so the rendered frames were dropped while the PNGs stayed on disk. Controller confirmed against the source and ruled FIX: test reachability BEFORE the destructive restore; an unknown card keeps its rendered frames and reports the missing snapshot as an ERROR. A paid render is never discarded silently.
  Send-back 2: D replied "Status: DONE" TWICE without implementing that guard (gui count stayed 268->275, _on_rendered byte-for-byte unchanged). Caught only because the controller verified the file instead of trusting the report. LESSON for the chain: verify an implementer's claim against the source every time; a DONE report is not evidence. Third dispatch landed it (gui 276, new test test_rendered_for_an_action_the_project_does_not_hold_keeps_the_frames asserts apply_frames is never reached, the render survives, and an ERROR is logged; reversion-checked).
  Stale billed_units docstring (C's cross-file catch) fixed by D, and D improved on the brief: it kept isdigit() and documented that the guard also keeps the count right for an OLDER project directory whose plates still sit beside the frames.
Fix wave COMPLETE: a9703b5 (A), 0a655b4 (B), d51c164 (C), dd48719 (D). All 8 Important + M9 + Minors 1, 2, 4, 5, 6 closed. Minor 3 (dialog deleteLater) deferred to 7 by ruling. Full suite + scoped re-review next.
Fix wave D — disputed timeline, settled with git evidence (recorded so the ledger stays factual): D objects that Item 1 was implemented before the third dispatch and that the controller had read committed HEAD d51c164 rather than the working tree. Both halves of that explanation are wrong on the mechanism — the controller read the working tree (sed on the path, which includes uncommitted edits), and d51c164 is C's commit, which touches ZERO GUI files (verified: `git show --stat d51c164` has no image_route_dialog entry). The decisive evidence is D's own reported gate counts: report 2 said "tests/sprite/gui 275 passed (268 before: 7 added, 1 flipped)" and report 3 said 276 with test_rendered_for_an_action_the_project_does_not_hold_keeps_the_frames added. A test that appears only at report 3 did not exist at report 2, so neither did the guard it covers.
  Outcome is correct either way and no further round is needed: the guard is committed at dd48719 gui/sprite/image_route_dialog.py:399, the new test is present, and D re-ran the reversion check (revert -> FAILED, restore -> PASSED). The chain LESSON stands unchanged and is the reason the discrepancy surfaced at all: verify an implementer's claim against the source every time; a DONE report is not evidence, and neither is an implementer's account of what it did earlier.
Full suite at dd48719 (post-fix-wave, controller-run 2026-08-30 19:41): **1903 passed, 19 skipped, 2 warnings in 216.26s**, exit 0. Baseline at 4567f8a was 1887 passed / 19 skipped / 2 warnings, so the wave added 16 net tests and regressed nothing. The 2 warnings are the same pre-existing third-party protobuf ones from test_main_window_sprite_wiring.py. Working tree clean under core/ gui/ tests/.
Scoped re-review dispatched as a dynamic Workflow (run wf_793cee39-2f0, task wp2f3b2hz): 4 closure groups (one per implementer commit) + 3 regression sweeps (regression, test-integrity, hard-rules/seams) -> 2-lens verify -> synthesize to image-route-rereview.md. Scope is deliberately narrow: is each finding closed AT THE ROOT, and did a fix break something else. The context file forbids re-litigating the 8 findings the final review refuted and the 2 settled rulings (one cancel poll; deferred deleteLater), so a second round cannot be spawned on already-decided ground. The test-integrity sweep independently re-performs the reversion check on >=6 of the 12 new tests in scratch copies, because every implementer CLAIMED reversion-checking and one implementer's claims proved unreliable.
