# Scoped re-review — sub-project 6 final-review fix wave

**Reviewer:** rereview-ir
**Range:** `4567f8a..dd48719` — 4 commits, 13 files, +531/-55
**Date:** 2026-08-30
**Method:** Read the range diff, the final review, `progress.md`, and the four implementer reports. Treat every implementer claim as unverified. Verify each closure against the source at HEAD `dd48719`, not against the new test. Run direct probes against the real functions for the export manifests, the GIF sidecar, the pose-step resolver, the retouch branches, the plate layout, and the logging sinks. Run 16 source reversions in a scratch copy of HEAD to prove that each new test fails when its fix is removed. Run the seven touched test files in the foreground with `QT_QPA_PLATFORM=offscreen`. Never modify the repository.

**Answer to the question this document exists to answer: every finding from the final review is closed.** All 8 Important findings, M9, the lost-render guard, the `billed_units` docstring, and Minors 1, 2, 4, 5 and 6 are closed at HEAD `dd48719`. No new Critical, Important or Minor finding survives verification. Three closures fix the symptom the review named rather than the underlying duplication (Important 2, Important 7, Important 8 guard half); each residual is unreachable at HEAD or is a pre-existing design gap, so none blocks the wave. The residuals appear in the Assessment as carry-forward notes for sub-project 7.

## Verification actually run (not claimed)

| # | Check | Command or probe | Result |
|---|---|---|---|
| 1 | Manifest equals disk for every preset, not only the tested ones | Call `export_with_preset` for all 8 presets, compare the returned list to a recursive `rglob` of the output directory | Exact match for all 8: godot4 5/5, libgdx 4/4, phaser3 4/4, pixijs 4/4, rpgmaker_mz 3/3, unity 4/4, unreal 4/4, web_preview 12/12; missing=[] extra=[] duplicates=0 |
| 2 | GIF sidecar frame count equals the real GIF | Export `web_preview`, reopen each GIF with PIL and read `im.n_frames`, for 4 directions x tag lengths 1..5 | 20 of 20 cases agree, including pingpong n=4 (6) and pingpong_reverse n=5 (8); `durations_ms`, `loop`, `warnings`, `timestamp` all survive the merge |
| 3 | The golden `.tres` comparison detects a line-structure regression | Patch `render_godot_tres` to drop the blank separator lines (38 lines to 33), then run `tests/sprite/test_godot_tres.py` | `1 failed, 8 passed`; the golden test is the sole detector. Two more line-structure regressions ("join whole file", "join each atlas block") also fail the new comparison and pass the old one |
| 4 | Pose-step model resolution returns a real model id | Call `default_chat_model` for every id `get_all_provider_ids()` returns and every alias `normalize_provider` accepts | openai -> `chat-latest`, anthropic -> `claude-sonnet-5`, gemini -> `gemini-3.7-flash`, Google -> `gemini-3.7-flash`, claude -> `claude-sonnet-5`; no family name returned. The pre-fix expression `resolve_model('gemini','chat')` returns the literal `chat` |
| 5 | Retouch neighbour provenance matches the dispatch | Run all four retouch branches with a mock provider, read back the images sent, the prompt text, the logged params and the persisted sidecar | google+region 1 image / no "neighboring" / 0 params / 0 sidecar refs; the other three branches 3 images / "neighboring" / 2 params / 2 sidecar refs |
| 6 | Retouch logs exactly once on the default path | Attach a root handler, call `retouch_frame` with no `log=` for google and openai, and force the `call_provider` error path | 1 request record and 1 response record per call, both named `core.sprite.generation.retouch`; 1 "failed:" record on the error path; `image_route`'s own callers still log once each |
| 7 | Matte plates are invisible to the pipeline | Run the real `edit_chain` with 3 matte steps into a real `stages/a1/extract` directory, then call `pipeline.list_frames` | `list_frames` returns exactly `0001.png, 0002.png, 0003.png` and equals the `edit_chain` return; every plate carries its own sidecar; every sidecar plate path exists |
| 8 | No other reader walks a stage directory recursively | Grep `rglob`, `glob(`, `iterdir()`, `listdir`, `walk(` across `core/`, `gui/`, `cli/` | No recursive stage reader. The only `rglob` on a stage tree is `core/sprite/project.py:578`, which counts files before it recycles the whole tree |
| 9 | The four bare `log()` calls now survive a raising sink | Run `edit_chain`, `generate_sheet` and `slice_generated_sheet` with a sink that raises on every call | All three complete; the module logger holds every progress line. `grep -n "^\s*log(" core/sprite/generation/` returns only `_common.py:24`, which is `emit`'s own guarded sink call |
| 10 | The two busy guards use a surface that exists | Read `gui/sprite/frames_workspace.py:56`, `gui/sprite/workers.py:252-277`, `gui/sprite/sprite_tab.py:71-75` | `FramesWorkspace.panel` is assigned once and never rebound, and the workspace is built before `install_retouch` and `install_image_route`, so neither guard can raise `AttributeError` |
| 11 | The M9 restore-then-apply order gives undo the pre-render list | Read `gui/sprite/image_route_dialog.py:396-402`, `gui/sprite/frames_workspace.py:289`, `core/sprite/undo.py:24-26` | `apply_frames` snapshots `action.frames` as it finds it, and `FrameListSnapshot.capture` deep-copies, so the snapshot holds the restored pre-render list and is not aliased |
| 12 | The lost-render reachability test cannot disagree with `apply_frames` | Compare `image_route_dialog.py:399` with `FramesWorkspace._find_action` at `frames_workspace.py:133-137` | Identical predicate over the same `tab.current_project` on the same synchronous call; `known` is True exactly when `_find_action` returns non-None |
| 13 | Every new or flipped test fails when its fix is reverted | 16 source reversions in a scratch copy of HEAD, one at a time, then re-run the owning file | All 16 produce a failure in the test that claims to guard the fix. Reversion 9 raises the production `IndexError` at `gui/sprite/retouch_wiring.py:28`; reversion 12 raises `OSError: [Errno 39] Directory not empty`; reversion 5 shows the same line under two logger names |
| 14 | The flipped golden test is a strengthening, not a cosmetic change | Change `core/sprite/exporters/godot_tres.py:107` from `"\n".join(parts)` to `" ".join(parts)`, then run the new test and the re-inserted old test | The new line-by-line test FAILS; the old `_norm` test PASSES (9 passed) |
| 15 | No hand-built data path entered the wave | `pytest tests/test_no_hardcoded_paths.py -q -p no:cacheprovider` | 3 passed; the guard rglobs `core`, `gui`, `cli`, `providers`, so it covers every file the wave touched |
| 16 | No new model-ID literal | Grep `core/sprite/` and `gui/sprite/` for quoted `gpt-*` / `claude-*` / `gemini-*` / `dall-e-*` | Only the two permitted `MODEL_CAPS["gpt-image-1"]` keys and the pre-existing `resolve_model(static_default=...)` triple in `action_cards.py:233-235`. `grep` for a model literal in `pose_steps.py` exits 1 |
| 17 | Every new user-facing refusal is logged and shown | Read `image_route_dialog.py:425-426`, `:404-407`, `retouch_wiring.py:28-31`, `:48-49` | All four call `logger.warning` or `logger.error` and `tab.console.log(...)` with a WARNING or ERROR level |
| 18 | All 55 deleted lines are accounted for | `git diff | grep '^-'` over the range, then trace each removed test | No orphan removal. The two deleted tests have strictly stronger successors: `_lines` replaces `_norm`, and one GUI test becomes three |
| 19 | Worker and thread hygiene in the new GUI tests | Run both touched GUI test files under a `sessionfinish` plugin that reads `_LIVE_ORPHANS` and `threading.enumerate()` | `ORPHANS: []`, `PY-THREADS: []`, 34 passed. The two busy-guard tests passed 8 consecutive runs; every wait carries a timeout |
| 20 | The touched tests pass at HEAD | `pytest tests/sprite/test_engine_presets.py tests/sprite/test_godot_tres.py tests/sprite/test_image_route.py tests/sprite/test_pose_steps.py tests/sprite/test_retouch.py tests/sprite/gui/test_image_route_dialog.py tests/sprite/gui/test_retouch_dialog.py -q -p no:cacheprovider` | 109 passed in 39 s |
| 21 | The sprite suite passes at HEAD | `pytest tests/sprite -q -p no:cacheprovider` | 825 passed, 2 pre-existing protobuf warnings, 142 s |
| 22 | The repository is unmodified | `git status --porcelain` and `git log -1` after all probes | HEAD is still `dd48719`; only Leland's pre-existing unrelated items appear |

## Closure per finding

| Finding | Closed? | Root-cause fix? | Test discriminating? | Evidence (file:line) |
|---|---|---|---|---|
| **Important 1** — the PNG-sequence manifest omits every per-frame sidecar | Yes | Yes | Yes | `core/sprite/exporters/engine_presets.py:168-170` pairs every PNG with `sidecar_path(png)`. Disk probe over all 8 presets matches exactly; `web_preview` returns 12 where it returned 8. The sibling writers are correct as they stand: `_write_aseprite_json:151-154` and `_write_texturepacker_json:157-160` write no sidecar and return `[out]`; `_write_godot_tres:163-165` and `_write_aseprite_native:193-196` return the pair; `_grid_output_paths:209-225` lists all three grid files. No sixth instance exists. Test at `tests/sprite/test_engine_presets.py:130-138` asserts full list equality against disk, so the pre-fix return drops 4 of 12 entries and fails. |
| **Important 2** — the GIF sidecar states a frame count the GIF does not have | Yes | No | Yes | `core/sprite/exporters/engine_presets.py:182-188` reads `export_gif`'s sidecar, merges, and rewrites. 20 of 20 PIL read-backs agree, and no `export_gif` field is lost. The fix is not at the root: line `:185` still re-derives the count from `ordered_frame_indices` (`godot_tres.py:41`, plain arithmetic) while `export_gif` derives it from `ordered_frames` -> `SheetMeta.frames_for` (`models.py:158-162`, a clamping slice). A hand-built out-of-range tag still over-reports. No caller can build such a tag — see Refuted finding 1. Test at `tests/sprite/test_engine_presets.py:141-159` fails on the old arithmetic (4 instead of 6) and fails again if the merge reverts to a plain write. |
| **Important 3** — pose-step model resolution returns the literal string `chat` | Yes | Yes | Yes | `core/sprite/generation/pose_steps.py:156` reads `model = model or default_chat_model(provider)`, imported at `pose_steps.py:17`; the `resolve_model` import is gone. Every provider and alias returns a real registry id. The literals stay in `action_cards._CHAT_FAMILY:232-236`, so no model id entered `pose_steps.py`. The seam sub-project 7 consumes is intact: `image_route.py:19` re-exports the same function object with an identical signature. Test at `tests/sprite/test_pose_steps.py:138-149` patches nothing and asserts the id is not `chat`; the pre-fix expression returns `chat`, so the test fails against the old code. |
| **Important 4** — the Gemini region retouch claims neighbours it never sends | Yes | Yes | Yes | `core/sprite/generation/retouch.py:149` computes `sent_neighbors`, and the prompt (`:151`), the logged params (`:152`) and the sidecar (`:189`) all derive from that one name. The condition matches the dispatch at `:158-173` exactly, so the only branch that reports zero neighbours is the only branch that sends zero. Tests at `tests/sprite/test_retouch.py:90` and `:106` pin both halves; the control test fails if the fix is made over-broad. |
| **Important 5** — retouch double-logs every request and response | Yes | Yes | Yes | `core/sprite/generation/image_route.py:75`, `:89` and `:97` add a `logger` parameter whose default binds at def time, so no existing `image_route` caller changes. All five retouch call sites pass it (`retouch.py:159`, `:162`, `:165`, `:170`, `:172`, `:176`), and `grep` finds no missed site. `_common.py:21` then skips the sink because `log.__self__ is logger`. Record counts: 1 request and 1 response per call on both providers, 1 record on the error path. Test at `tests/sprite/test_retouch.py:118` asserts the counts and the record-name set, so dropping any one keyword fails it. |
| **Important 6** — matte plates land in the pipeline's extract directory | Yes | Yes | Yes | `core/sprite/generation/image_route.py:324` sets `plates_dir = out_dir / "plates"`, and `:325-326` save there. `core/sprite/pipeline.py:145` is unchanged and non-recursive, so the sub-project 1 contract holds. The probe shows `list_frames` returns only the composed frames. Every plate carries a sidecar (`image_route.py:327-332`), and every sidecar plate path and chained reference resolves. No other reader is recursive; `extract.py:284` and `pipeline.py:316` both `rmtree`, and `archive_existing_frames` renames the whole tree (`image_route_dialog.py:52`). Test at `tests/sprite/test_image_route.py:287` fails on the old location, where `list_frames` returns 6 paths. |
| **Important 7** — "Render (image)" has no busy guard | Yes | No | Yes | `gui/sprite/image_route_dialog.py:422-427` refuses on `tab.frames_workspace.panel.is_busy()`, logs a warning and shows a console WARNING, in the exact shape of `gui/sprite/frames_workspace.py:322-327`. The panel always exists (`frames_workspace.py:56`, `sprite_tab.py:71-75`), so the guard cannot raise. Root-cause is "No" only because the guard covers one of the tab's four writer hosts; `QueuePanel` is a second host — see Refuted finding 3. Test at `tests/sprite/gui/test_image_route_dialog.py:379` drives a real `ProcessingPanel` and a real gated `SpriteWorker`; the mutant with the guard removed fails. |
| **Important 8 (guard half)** — frame-strip "Retouch…" has the same missing guard | Yes | No | Yes | `gui/sprite/retouch_wiring.py:45-50` mirrors the same guard, and `install_retouch` runs after the workspace exists (`sprite_tab.py:71-73`). The busy test precedes the frame test at `:51`, so a busy panel plus an absent frame reports the busy message; that order is correct, because the running job is rewriting the frame list and no state changes on either branch. Root-cause is "No" for the same single-host reason as Important 7. Test at `tests/sprite/gui/test_retouch_dialog.py:263` uses a real panel and worker; the mutant fails. |
| **Important 8b** — `apply_retouch` indexes a list the worker can shorten | Yes | Yes | Yes | `gui/sprite/retouch_wiring.py:27-32` re-validates `0 <= index < len(action.frames)` as the first statement, before the deepcopy and before the subscript at `:34`, then logs and shows and returns. The only caller is the `dialog.retouched` connection at `:62`, a Qt slot, so no `IndexError` escapes a slot. Test at `tests/sprite/gui/test_retouch_dialog.py:229` fails with the production `IndexError` when the guard is removed. |
| **M9** — a render of an unselected card pushed no undo snapshot | Yes | Yes | Yes | `gui/sprite/image_route_dialog.py:396-402` drops the `tab.current_action()` branch, captures the rendered list, restores `dialog.frames_before`, then calls `apply_frames` for every card. `frames_workspace.py:289` and `core/sprite/undo.py:24-26` show the snapshot deep-copies the restored list, and `dialog.frames_before` is itself a deepcopy taken at `:298`, so nothing aliases a live `FrameMeta`. `apply_frames` passes `reload=False` for a non-current card (`frames_workspace.py:290-291`). Test at `tests/sprite/gui/test_image_route_dialog.py:321` pins the snapshot size, so it fails if the restore is dropped and fails if the unselected card skips `apply_frames`. |
| **Lost-render guard** — `apply_frames` returns early after the destructive restore | Yes | Yes | Yes | `gui/sprite/image_route_dialog.py:398-407` evaluates `known` before the restore and runs the restore and `apply_frames` only inside `if known:`. The unknown branch keeps the rendered frames, logs an error and shows an ERROR. `known` uses the same predicate as `FramesWorkspace._find_action` (`frames_workspace.py:133-137`) over the same project on the same synchronous call, so the two cannot disagree. Test at `tests/sprite/gui/test_image_route_dialog.py:340` fails under both the old `_on_rendered` shape and a restore-after-apply mutant. |
| **`billed_units` docstring** — credited the plates' non-numeric stem | Yes | Yes | Yes (behaviour only) | `gui/sprite/image_route_dialog.py:58-68` now names the real mechanism: `edit_chain` writes `NNNN.png` into `extract_dir` and the plates into `extract_dir/plates`, and the glob at `:74` is non-recursive, so it never reaches them; the `isdigit()` filter is named as a defensive guard for a legacy project directory. HEAD agrees: `image_route.py:324-326` and `:294`/`:336`/`:341`. Docstring text cannot be test-enforced, but the counts are pinned at `tests/sprite/gui/test_image_route_dialog.py:198` and `:204`. |
| **Minor 1** — four progress lines bypass `emit` | Yes | Yes | No | All four route through `emit`: `core/sprite/generation/image_route.py:206`, `:225`, `:227`, `:355`. `grep -n "^\s*log(" core/sprite/generation/` returns only `_common.py:24`. A raising-sink probe shows all three call paths complete and every line reaches the module logger. The wave added no test for Minor 1; the four lines are covered only by existing tests whose sinks never raise. The final review prescribed the `emit` conversion alone, and a coverage gap on obviously-correct three-argument calls stays below the Minor bar, so no finding is raised. |
| **Minor 2** — no cancel poll inside the matte plate loop | Yes | Yes | Yes | `core/sprite/generation/image_route.py:298-301` polls the token at the top of the plate loop. `progress.md:88` rules the poll stays a single poll, because a second poll after `log_response` fires only on a step's last plate and would discard a frame the user paid for. Test at `tests/sprite/test_image_route.py:314` asserts one provider call; the mutant with the poll removed fails with `assert 2 == 1`. |
| **Minor 4** — the golden `.tres` comparison collapses every newline | Yes | Yes | Yes | `_norm` is gone. `tests/sprite/test_godot_tres.py:30-37` defines `_lines` as `[line.rstrip() for line in text.splitlines()]`, used by the golden comparison at `:44`. Three independent line-structure regressions each pass the old comparison and fail the new one. The `rstrip`/`splitlines` pair keeps the trailing-whitespace and EOF tolerance, so the fix is at the root rather than a narrower special case. The golden test is the only detector in its module. |
| **Minor 5** — `billed_units`' sheet branch and matte doubling have no test | Yes | Yes | Yes | Three direct unit tests exist at `tests/sprite/gui/test_image_route_dialog.py:192`, `:198` and `:204`, and `billed_units` is imported directly at `:19-21`. Reversion of the `isdigit()` filter fails two of them plus the pre-existing ledger test; reversion of the sheet branch fails `:192` with `assert 1 == 0`; reversion of the matte doubling fails `:204` with `assert 2 == 4`. |
| **Minor 6** — the archive same-second collision loop is unreachable from any test | Yes | Yes | Yes | `tests/sprite/gui/test_image_route_dialog.py:162` freezes the only `datetime` use (`gui/sprite/image_route_dialog.py:43`), archives twice inside the frozen second, and asserts the exact serial names produced by `image_route_dialog.py:48-51`. Removal of the serial loop fails the test with `OSError: [Errno 39] Directory not empty`, which is the harm the loop prevents. |

## New findings

### Critical

None.

### Important

None.

### Minor

None.

The wave introduces no defect that survives verification. Five candidates reached the verification stage; all five are refuted below.

## Refuted findings

1. **The GIF sidecar frame count is still derived independently of the exporter, so a clamped tag over-reports** (`core/sprite/exporters/engine_presets.py:185`) — the arithmetic divergence is real, but no caller can build an out-of-range tag. `SheetMeta.from_dict` and `TagMeta.from_dict` (`core/sprite/models.py:143`, `:101`) have zero non-test callers, `SpriteProject.to_dict` stores no tags, and the sole production construction site (`core/sprite/project.py:549`) sets `to_index=len(frames) - 1` while it appends those frames. The failure scenario needs a hand-built `SheetMeta`.
2. **Ollama and LM Studio now raise `ValueError` in the pose-step provider combo** (`core/sprite/generation/pose_steps.py:156`) — no capability is lost. The pre-fix call returned the literal `chat`, which LiteLLM rejects, so pose steps never worked for those two providers. `SpriteWorker.run` catches the error, logs it with a traceback and shows it (`gui/sprite/workers.py:124-126`), and the dialog restores the card. The wave changes the error text, not the outcome.
3. **The two busy guards cover only `frames_workspace.panel`; `QueuePanel` is a second writer host** (`gui/sprite/image_route_dialog.py:422`) — the concurrency window is real and confirmed, but the final review named one writer and prescribed the `open_export_dialog` mirror, `progress.md:90` and `:96` record that mirror as the ruled remedy, and the gap is pre-existing rather than introduced by `dd48719`. It belongs in sub-project 7 as a tab-wide busy contract.
4. **Matte-mode cancel loses one paid plate call from the ledger** (`gui/sprite/image_route_dialog.py:74`) — this is the triage row the final review already ruled **defer** (`image-route-final-review.md:445`), at the same helper and with the same one-call cap. The controller also weighed the plate poll against `billed_units` when it accepted Fix C (`progress.md:88`).
5. **The plan still documents the pre-wave matte-plate location with no deviation entry** (`Plans/2026-08-29-sprite-image-route-exports-plan.md:2410`) — the plan text is stale, but plan bookkeeping is the plan's own unrun Task 11, the deviation is recorded in `progress.md:87`, and sub-project 7 reads the plate paths from each frame's sidecar (`core/sprite/generation/image_route.py:344-350`), not from plan prose. The same staleness applies equally to Fix A and Fix B, so it is not a Fix-C regression.

## Assessment

**Approved.**

Every finding the final review left open is closed at HEAD `dd48719`, and every closure is verified against the source rather than against the new test. No confirmed Critical, Important or Minor finding remains. The touched test files pass (109 passed), the sprite suite passes (825 passed), and 16 reversion mutants prove that no new or flipped test can pass with its fix removed.

Three items carry forward to sub-project 7. None blocks this wave, and none needs a fix round now.

1. **Tab-wide busy contract.** `SpriteTab` owns four writer hosts (`gui/sprite/sprite_tab.py:265-274`), and the accepted guard shape covers one. A single guard over `_worker_panels()` closes the class. Carry it with the deferred Minor 3 (`deleteLater`).
2. **`export_with_preset` seam notes for the CLI brief.** The manifest now interleaves artifacts with `.json` sidecars, so a `--json` manifest must partition by suffix or it double-counts. `FORMAT_IDS` (`core/sprite/exporters/engine_presets.py:22`) lists seven ids while `FORMAT_WRITERS` (`:199`) holds six, because `export_with_preset` handles `grid` in the `ATLAS_FORMATS` branch (`:239-242`); a CLI that dispatches through `FORMAT_WRITERS` gets `None` for `grid`. There is no per-format entry point, and `aseprite_native` belongs to no preset.
3. **Logging-helper contract for CLI callers.** `log_request`, `log_response` and `call_provider` dedupe only when the caller passes `logger=` alongside a logger-bound `log=` sink (`core/sprite/generation/_common.py:21`). A CLI module that passes `log=cli_logger.info` without `logger=cli_logger` writes two records per request. State the rule in the sub-project 7 brief: pass a plain callable as `log`, or pass `logger=` with a logger-bound sink.
