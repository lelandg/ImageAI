# SDD ledger — plan: /mnt/d/Documents/Code/GitHub/ImageAI/Plans/2026-08-29-sprite-gui-b-plan.md
Chain position: 5b of (1 ✓ → 2 ✓ → 3 ✓ → 4 pixel-art (running) → 5a → 5b → 6).
Spec: Plans/2026-08-29-sprite-tab-design.md §1.5, §3, §4.6, §5 (reachable).
Pre-flight scan: preflight-scan.md — 0 MISMATCH; sub-project-6 seam list matches byte-for-byte; 2 CONFLICT + 1 rubric duplicate.
Ruling (Task 6 dispatch): ProcessingPanel._probe_clip's worker.failed handler must also emit logMessage (status console) with the user_message, not only logger.warning — AGENTS.md: every user-facing error is logged AND shown. Cost if wrong: one extra console line.
Ruling (Task 4 dispatch): FrameStrip._rebuild decodes every frame as a full QPixmap on the UI thread — decode thumbnails with QImageReader.setScaledSize at strip-cell size and cache by (path, mtime); the thumbnail is scaled proportionally (KeepAspectRatio). Cost if wrong: ~15 lines; avoids a UI stall on 512-frame actions.
Ruling (Task 1 dispatch): UndoController._copy must reuse core.sprite.undo.FrameListSnapshot.capture instead of re-implementing the deep copy. Cost if wrong: none.
Note: extra shortcut Ctrl+Shift+Z → redo is additive to §1.5 — LEAVE.
Note: core-spine parked slicing hazard — not reachable from any 5b dialog (no slice_sheet/import_png_sequence call).
Carry-forward (5b Task 6 ProcessingPanel): after fix I1, palette_lock=False no longer auto-rebuilds every run — it only gates whether an explicit 'Rebuild palette' action is allowed; the panel must expose that action (pixelart.rebuild_palette) and refresh the pixel stage.
Environment fact (from 5a Task 1): PySide6 6.11.1 delivers SpriteWorker cross-thread signals as Queued — GUI tests must pump QApplication.processEvents() after worker.wait()/shutdown() before asserting on lambda-connected signals.
Chain position update (2026-08-30): 1 ✓ → 2 ✓ → 3 ✓ → 4 ✓ → 5a ✓ (64a9673..0ec7ce9, suite 1637+) → 5b (this) → 6.
Carry-forward from 5a final review (deferred to 5b — triage each when the owning task is dispatched):
- Minor 1 / deviation 16: CharacterPanel has no Ctrl+Enter primary action (two equal actions) — install_shortcuts decides the binding.
- Minor 8: QueuePanel._on_progress re-emits every ActionQueue log line as INFO (queue warnings/errors show as INFO in the console; file logger has the true level) — give SpriteWorker a level-aware log path or encode the level in the stage name.
- Minor 4 (interim): SpriteProject.save() takes a process-wide RLock; the GUI-only-writer redesign (ActionQueue reports "save requested" via progress; QueuePanel autosaves on the GUI thread) is 5b/6 work if ActionQueue is touched.
- Minor 10: persisted current-tab index is positional; the Sprite tab insert at index 4 shifts a saved current_tab=4 (Settings) once — persist by tabText if a tab is inserted again.
- Minor 11: MainWindow wiring tests assert on inspect.getsource text — a headless MainWindow construction test would replace them.
- Pre-existing: WorkerHost._release_worker never deleteLater()s a normally finished worker (one dead QThread child per job) — tests/panels inspect the worker after delivery, so add deleteLater together with a wait_for_worker change.
- T4: CharacterPanel.set_project runs analyze_source on the GUI thread (~10 ms; a huge PNG stalls briefly) — move into a worker if a 5b task touches set_project.
- T9: MainWindow._on_send_to_sprite TypeError branch logs but does not show_error (unreachable from the shipped surfaces) — add show_error if a non-Path caller appears.
- MANUAL (Leland, Windows PowerShell): click-through of the three "Send to Sprite" surfaces + lazy Sprite-tab load (plan 5a Task 9 Step 7 left unchecked).
Environment fact (5a): WorkerHost.shutdown() now returns bool and orphans a worker that outlives the bounded join; is_busy() stays True until the orphan is reaped; panels override _on_worker_idle() to re-sync. SpriteTab.shutdown() -> bool, SpriteTab.join_orphans(). Any new 5b panel that subclasses WorkerHost must override _on_worker_idle.
Task 1: dispatched (impl-gb-1, sonnet) at BASE 45f9796 — 2026-08-30
Task 2: dispatched (impl-gb-2, sonnet) in parallel with Task 1 at BASE 45f9796 — Ruling: Task 2 must not create/overwrite tests/sprite/gui/gui_synthetic.py (Task 1 owns it); if absent when Task 2 runs its tests, wait/poll for it rather than writing it. Cost if wrong: a merge race on one helper file.
Task 2: implemented 5d15c5f; review dispatched (rev-gb-2) — package review-45f9796..5d15c5f.diff
Task 3: dispatched (impl-gb-3, sonnet) at BASE c5abd21 — gui_synthetic.py now committed (c5abd21)
Task 1: implemented c5abd21 (capture() ruling applied); review dispatched (rev-gb-1) — package review-5d15c5f..c5abd21.diff
Task 4: dispatched (impl-gb-4, sonnet) at BASE c5abd21, in parallel with Task 3 — FrameStrip thumbnail ruling (QImageReader.setScaledSize + (path, mtime) cache, KeepAspectRatio) applies
Task 2: minor (deferred): fit_zoom() has no direct test (brief's own test list).
Task 2: ⚠️ carried to Task 9 (FramesWorkspace): PixelView.set_image() returns False on decode failure and only logs — the caller must show it (console line) per the logged-AND-shown rule.
Task 2: complete (commit 5d15c5f, review clean)
Task 1: minor (deferred): unused `Path` import in test_undo_controller.py (brief text); undo()/redo() deep-copy twice on the return path (brief's own shape) — clean up if Task 9 touches the controller.
Task 1: complete (commit c5abd21, review clean — assessment Approved, only Minors)
Task 5: dispatched (impl-gb-5, sonnet) at BASE c5abd21, in parallel with Tasks 3-4
Task 3: implemented e7b04ba; review dispatched (rev-gb-3) — package review-c5abd21..e7b04ba.diff
Task 4: implemented bc1f8fd (thumbnail ruling applied); review dispatched (rev-gb-4) — package review-e7b04ba..bc1f8fd.diff
Task 6: dispatched (impl-gb-6, opus — threading-sensitive WorkerHost panel) at BASE 425cc5d, parallel with 5/7 — rulings: probe failure → logMessage + logger; expose 'Rebuild palette' (pixelart.rebuild_palette) gated by palette_lock; override _on_worker_idle (5a WorkerHost orphan contract)
Task 7: dispatched (impl-gb-7, sonnet) at BASE 425cc5d, parallel with 5/6 — rulings: purge-after-export via prefs.purge_after_export_enabled/confirm_purge (5a Task 3); override _on_worker_idle; register_format seam byte-for-byte for sub-project 6
Task 3: Ruling: plan-mandated Important (PreviewPlayer._pixmap decode failure logged but not shown — brief's own prototype) downgraded to ⚠️ carried to Task 9: FramesWorkspace must surface PixelView.set_image()==False and PreviewPlayer decode failures in the tab console (add a small signal on PreviewPlayer in Task 9 if needed). Cost if wrong: a silent blank preview on a corrupt frame until Task 9 lands.
Task 3: minor (deferred): loop_seam_score mismatched-size branch untested; no hideEvent pause; reverse non-wrap step untested.
Task 3: complete (commit e7b04ba, review clean)
Task 5: implemented 425cc5d; review dispatched (rev-gb-5) — package review-bc1f8fd..425cc5d.diff
Task 8: dispatched (impl-gb-8, sonnet) at BASE 425cc5d, parallel with 6/7 — tests use a SimpleNamespace tab, no Task 9 dependency; note Ctrl+Shift+Z→redo additive (LEAVE)
Task 4: Ruling: plan-mandated Important (insert_from_file stores the external path; brief's own code) ruled FIX — copy the PNG into the project next to the neighbouring frames' source dir (`<stage dir>/inserted/<stem>_<n>.png`, project-relative; refuse with a shown+logged message when the strip has no frames to anchor on); the FrameMeta points at the copy. Cost if wrong: one copied file per insert.
Task 4: minor (deferred): FrameOverridesDialog hardcodes the three OVERRIDE_KEYS names; _finish_reorder defensive branch can leave a no-op snapshot.
Task 4: fix round 1/5 dispatched to impl-gb-4.
Task 5: minor (deferred, FYI): title-bar close persists the splitter before the reject() guard runs (documented DialogCleanupMixin caveat; idempotent).
Task 5: complete (commit 425cc5d, review clean)
Task 4: fix round 1/5 (1 addressed — a88b96c copies inserted PNGs into the project; 15 tests); re-review dispatched (rev-gb-4) — package review-425cc5d..a88b96c.diff
Task 8: implemented 77fcbaf; review dispatched (rev-gb-8) — package review-a88b96c..77fcbaf.diff
Task 4: complete (commits bc1f8fd..a88b96c, re-review clean)
Task 8: complete (commit 77fcbaf, review clean)
Task 6: implemented 0bfc249 (rulings A/B/C applied; deviations: probe worker parented + _release_probe reap, lambdas → named slots, no direct _worker writes); review dispatched (rev-gb-6, opus) — package review-77fcbaf..0bfc249.diff
Task 6: ⚠️ cross-task: Task 7's uncommitted export_dialog.py segfaults under a bare pytest tests/sprite (worker running while dialog is GC'd) — forwarded to impl-gb-7 before its commit.
Task 6: Ruling: Important 1 (probe worker has no identity guard — stale probe applied to the new action, new action never probed, queued _release_probe can wait() on the wrong worker) ruled FIX: bind every probe slot to (worker, action_id) via functools.partial and drop when `_probe_worker is not worker` or the action changed; set_action must supersede an in-flight probe (detach + orphan via _adopt_orphan, then probe the new clip) instead of returning early; _release_probe waits only on the worker it was bound to. Cost if wrong: a wrong frame estimate; a GUI stall for one ffprobe.
Task 6: fix round 1/5 dispatched to impl-gb-6 (full review text requested at task-6-review.md — reviewer output was truncated).
Task 6: full review at task-6-review.md (1 Important, 4 Minor). Minor 1 (orphan leaves run UI live — _on_worker_idle must _set_running(False)) folded into fix round 1. Minors deferred: duplicated pipeline job closure in run_pipeline/rebuild_palette_for; open_install_dialog has no error handling; invalid key colour logged but not shown (fires per keystroke — leave).
Task 7: implemented 105b65c (DONE_WITH_CONCERNS); review dispatched (rev-gb-7, opus) — package review-0bfc249..105b65c.diff
Task 7: ⚠️ SYSTEMIC (final review to triage): implementer reports a GC-vs-QThread segfault race in the shared 5a WorkerHost/SpriteWorker test pattern (dialog with a worker dropped without shutdown → Python GC deletes the QThread); worked around in test_export_dialog.py (_close() + gc.collect()). Candidate root fix: conftest wait_for_worker/teardown joins, or SpriteWorker keeps a module-level strong ref while running.
Controller ruling (from Task 7's residual flag): the 5a orphan list was a collectable Python cycle → module-level _LIVE_ORPHANS strong-ref set in gui/sprite/workers.py (commit 17fc754, GC regression test; 48 worker/export/tab tests green). Task 7's test-file gc.collect() workaround stays (it targets accumulated dead-dialog garbage, a different mechanism). Cost if wrong: one set entry per orphan.
Task 7: review at task-7-review.md — 0 Critical, 5 Important, 9 Minor. Ruling: all 5 Important FIX. Important 2 (GC segfault: dead QThread children of a parentless dialog) fixed at the root by the controller in gui/sprite/workers.py (fee96b5: _release_worker joins + setParent(None); deleteLater crashed the suite because the worker's own signal delivery is still on the stack; 181 GUI tests green). Task 9 must parent ExportDialog to the tab and hold the reference while open. Remaining Importants 1/3/4/5 + Minor "private _worker writes" → fix round 1 to impl-gb-7; other Minors deferred (dead format_grid branch, saved formats not applied to sub-project-6 registrations, global-not-per-project settings, GUI-thread purge, test-name mismatch, coverage breadth).
Task 7: fix round 1/5 (4 Important + 1 Minor addressed — 2fdd763; 21 tests); re-review dispatched (rev-gb-7) — package review-fee96b5..2fdd763.diff
Task 7: re-review at task-7-rereview.md — 5/6 addressed; Important 2 PARTIAL (fee96b5 removed the named object; 3/10 segfaults remain with the per-file gc.collect neutralised — the trigger is now the collection of earlier closed dialogs' widget trees during a later worker's Qt activity). New 1 (close test passes with the fix reverted) + New 4 (close test skips gc.collect) → fix round 2 to impl-gb-7. New 2 (unbounded join_orphans can freeze the UI for one format call — house pattern, same as sprite_tab) DEFER; New 3 (_grid_output_paths duplicates grid.py naming — export_grid should return its paths) DEFER to sub-project 7 note.
Task 7: Ruling: Important 2 residual — controller adds an autouse teardown `gc.collect()` fixture to tests/sprite/gui/conftest.py (collect only after the test joined its workers) so no test file needs its own copy; Task 9 must parent ExportDialog to the tab, hold the reference while open, and deleteLater it on close. Cost if wrong: one collection per GUI test (~ms).
Task 6: fix round 1/5 (Important 1 + Minor 1 addressed — 194e822: probe slots bound to a run id + action id, supersede via _adopt_orphan, _release_probe never reads _probe_worker; 21 tests, 12 serial runs clean); re-review dispatched (rev-gb-6) — package review-7077dc7..194e822.diff
Task 6: ⚠️ SYSTEMIC (final review to triage): WorkerHost.start_job connects functools.partial(..., worker) to the worker's own signals → a worker self-cycle; a finished, detached worker is freed only by the cyclic collector (an arbitrary GC pass on the GUI thread). Candidate: bind a run id instead of the worker object, or disconnect in _release_worker.
Task 6: fix commit amended 194e822 → 863c337 (adds _on_worker_idle run-UI reset + finally-joined gated tests; whole tests/sprite/gui 191 passed); re-review package rebuilt: review-7077dc7..863c337.diff
Task 6: re-review at task-6-rereview.md (judged 194e822 + WIP): Important 1 CLOSED (int run-id deviation accepted — a partial holding the worker on its own signals is a self-cycle); Minor 1 fix was uncommitted WIP then, now in 863c337 — stability check (5× file, 3× directory) requested from rev-gb-6 before close. New Minor (deferred): a superseded probe orphan keeps is_busy() True, so Run is disabled until the old ffprobe returns (sub-second); candidate: keep probe orphans out of the run-gating busy test.
Task 7: fix round 2/5 (New 1 + New 4 addressed, test-only — 6dab3a6; the committed close test fails against a broken on_dialog_close via out-of-tree module swap; 191 GUI tests, no segfault). Accepted on the implementer's evidence — no third review round.
Task 7: complete (commits 105b65c..6dab3a6 + controller fee96b5/7077dc7; re-review clean on the merits)
Task 9: dispatched (impl-gb-9, opus — integration, modifies 5a sprite_tab.py) at BASE 6dab3a6 — rulings: parent ExportDialog to the tab, hold the ref while open, deleteLater on close; surface PixelView.set_image()==False and PreviewPlayer decode failures in the tab console (Task 2/3 carry-forwards); SpriteTab.shutdown() must include the workspace's panel (ProcessingPanel) and return the AND; workspace panels override _on_worker_idle where they host workers.
Task 6: complete (commits 0bfc249..863c337, re-review Approved on 863c337 — 5× whole-directory runs green, one under CPU load; the earlier flake was a mid-edit artifact)
Task 10 (pre-run by controller while Task 9 runs): banned-pattern greps clean on Tasks 1-8 modules — the single 'QMovie' match is preview_player.py:3 docstring text ('No QMovie'), not a usage. Re-run after Task 9 lands.
Task 9: implemented 52faf1e (rulings A-D; deviations: set_view_image, _FakeDialog extended, decodeFailed signal in preview_player); review dispatched (rev-gb-9, opus) — package review-6dab3a6..52faf1e.diff. Named risk from the implementer: SpriteTab._shutdown_panel_workers does not include frames_workspace.panel on project switch (5a Task 8 class).
Task 10: guard greps clean with frames_workspace.py included; fresh-interpreter check: importing gui.main_window does not import gui.sprite (lazy load intact). Full suite running on 52faf1e (background).
Task 10: full suite on 52faf1e — 1751 passed, 19 skipped, 2 warnings in 203.29s (0:03:23)
Task 9: review at task-9-review.md — 0 Critical, 3 Important, 5 Minor. Ruling: Important 1 (project switch skips frames_workspace.panel — 5a Task 8 class) FIX; Important 2 (open_export_dialog returns a dialog destroyed as the modal loop unwinds — deleteLater posted inside exec()) FIX: hold the reference through exec(), deleteLater AFTER exec() returns, return a live object; Important 3 (apply_frames emits projectChanged with no autosave listener → re-entrant rebuild, no persistence) FIX: apply_frames calls the tab's _autosave() and the workspace's own projectChanged slot is idempotent for the same project identity. Minor 4 (apply_frames on a non-current action reloads the current widgets) folded into the fix round: reload strip/player only when action_id is the current action. Minors 5-8 deferred (set_view_image unused in production; decode-failure log levels differ; single-action fixture coverage; annotation-only QImage/QPixmap imports).
Task 9: fix round 1/5 dispatched to impl-gb-9.
Task 9: fix round 1/5 (3 Important + Minor 4 addressed — 157b282; 5 new tests; 215 GUI ×3; implementer measured that Important 2's stated cause does not reproduce on PySide6 6.11.1 but the ruled shape fixes a real held-forever-dialog defect); re-review dispatched (rev-gb-9) — package review-52faf1e..157b282.diff
Task 10: full suite on 157b282 — 1757 passed, 19 skipped, 2 warnings in 194.66s (0:03:14)
Task 9: re-review at task-9-rereview.md — 5/5 addressed; Important 2 WITHDRAWN by the reviewer (Qt deferred-delete rule mis-derived; both shapes leave the dialog valid on PySide6 6.11.1) — the shipped try/finally shape stays because it fixes a real held-forever dialog. New Minors (deferred to the final review's triage): open_export_dialog docstring + a test comment now cite the withdrawn rationale (must be corrected); Minor 4's test does not discriminate (refresh_frames heals the strip first); double strip/player reload per apply_frames; two divergent lists of the tab's worker hosts.
Task 9: complete (commits 52faf1e..157b282, re-review Approved)
Final review: dispatched as dynamic workflow (5 dimensions + deferred-minor triage → 3-lens adversarial verify → synthesize final-review.md) on review-45f9796..157b282.diff (17 commits, 24 files, +5706/-4). Task 10 plan close-out commit deferred until after the fix wave (5a pattern).
Final review: complete (wf_bca6d750-532, 88 agents; final-review.md) — 0 Critical, 7 Important, 13 Minor; triage promoted 6 items to fix-now.
Final review: Ruling: ALL 7 Important FIX — I1 §1.5 shortcut scope per owner widget; I2 worker leak (partial-in-connection self-cycle) → run-id binding in workers.py + disconnect + weakref test (overrides my earlier SYSTEMIC "defer"); I3 export gated on panel.is_busy(); I4 parse_scales refuses bad tokens (shown); I5 FrameOverridesDialog.accept() refuses invalid key colour (shown); I6 processing_panel Run/Preview refuse invalid key colour via _warn; I7 export tests isolate QSettings per test. Also fix-now: T7 register_format applies persisted formats; export out_dir keyed per project; Minor 2 (withdrawn rationale text), 3 (thumbnail docstring), 7 (thumbnail miss logged+shown once), 8 (openUrl return), 9 (mkdir in handler), 11/12/13 (test discrimination). Cost if wrong: small local edits, each with a test.
Final review: Ruling: DEFER to sub-project 6 brief — Minor 4 (helper dialogs deleteLater after exec), Minor 5 (superseded probe orphan gates Run), Minor 6 + T7 GUI-thread purge (recycle failure reported as success), Minor 10 (double decode log). Minor 1 (§4.5 "live re-run" reinterpretation) → recorded as a plan deviation by the controller, with the §1.5 scope correction.
Final review: fix wave 1/1 dispatched in parallel — fix-A (opus: workers.py run-id + I3 + Minor 2/11), fix-B (sonnet: shortcuts I1 + frame_strip I5/Minor 3/7/9), fix-C (sonnet: export_dialog I4/I7/T7-fix-nows + processing_panel I6/comment/Minor 8 + Minor 12/13). No commits by implementers; controller commits by path.
Final review: fix A done (uncommitted; workers.py weakref-bound partials + disconnect — deviation from the run-id ruling, keeps the wait/detach path on stale events; frames_workspace export gate via EnabledChange mirror of panel.export_btn; Minor 2/11 done; 233 GUI ×2). Awaiting B and C before the controller commits by path.
Final review: fix B done (uncommitted; B1-B5; module docstring corrected by controller to match Minor 3). Awaiting C.
Final review: fix wave committed — A/B/C as 8c0dafa,1b268f8,0559567; merged gate 784 passed; re-review package review-157b282..0559567.diff
Final review: scoped re-review dispatched as workflow wf_e03d61ab-366 (8 closure groups + 2 sweeps + 2-lens verify) on review-157b282..0559567.diff; closing full suite running in background.
Task 10: closing full suite on 0559567 — 1775 passed, 19 skipped, 2 warnings in 221.14s (0:03:41)
Final review: re-review complete (wf_e03d61ab-366, 18 agents): ALL 8 closure groups CLOSED, 0 confirmed new findings, 4 refuted. Residuals: processing_panel run-id docstring corrected by controller (8f571d4); dialog-side key-colour refusal has no console line (FrameOverridesDialog has no logMessage) — noted for 6; corrupt-path warn-once count unpinned — polish.
Task 10: plan close-out committed (9898946): all checkboxes ticked, Last Updated set, deviations 13-14 recorded.
Final review: CLOSED 2026-08-30 16:19 — closing full suite 1775 passed / 19 skipped / 0 failures (0559567). Sub-project 5b complete: 45f9796..9898946. Workspace archived into the image-route (6) workspace and removed.
