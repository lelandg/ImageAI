-c requirements-security.txt

protobuf>=5.29.6,<6  # Patched recursion limit; modern MediaPipe Tasks has no protobuf<5 pin
pillow>=12.3.0
numpy>=1.26.4  # Explicit safe floor for MIDI and image processing dependencies
packaging>=26.3  # Runtime version/specifier validation for optional ML backends
google-genai>=2.9.0  # 2.9.0+: GeminiNextGen Interactions API w/ output_video (Gemini Omni video). NBP image_size (1K/2K/4K) still supported.
google-cloud-aiplatform
PySide6
openai
requests>=2.33.0  # For Stability AI REST API
markdown>=3.5  # For better help documentation rendering
keyring>=24.0.0  # For secure API key storage (optional)

# Optional dependencies for Local Stable Diffusion
# Uncomment these lines to enable local SD support:
# torch>=2.0.0  # Note: Use CUDA version for GPU support
# diffusers>=0.24.0
# transformers>=4.35.0
# accelerate>=0.24.0
# safetensors>=0.4.0
# psutil>=5.9.0

# Video Project Feature Dependencies
Jinja2>=3.1.0  # Template processing for prompt generation
imageio-ffmpeg>=0.4.9  # FFmpeg Python bindings
litellm>=1.100.1,<2  # Unified LLM provider interface
anthropic>=0.25.0  # Claude API support (optional for prompt generation)

# MIDI Processing & Synchronization  
setuptools>=83.0.0  # Build dependency for pretty-midi
pretty-midi>=0.2.11.post0  # High-level MIDI analysis for timing extraction (latest available version)
mido>=1.3.2  # Low-level MIDI manipulation for detailed control

# Optional dependencies for Local LLM support
# Uncomment if you want to use local LLMs for prompt generation:
# ollama>=0.3.0  # Local LLM support via Ollama

# Video creation dependencies (required for video projects)
opencv-python>=4.8.1.78  # Required for video frame processing and assembly

# Sprite tab de-jitter (core/sprite/stabilize.py); OpenCV phaseCorrelate is the fallback
scikit-image>=0.24  # skimage.registration.phase_cross_correlation (sub-pixel frame alignment)
scipy>=1.13  # required by scikit-image

# Discord Rich Presence (optional - for showing activity in Discord status)
pypresence>=4.6.0  # Discord RPC wrapper library

# Font Generator Dependencies
fonttools>=4.60.0  # Font file creation (TTF/OTF) from vectorized glyphs

# AI Upscaling (installed at runtime via GUI if needed)
# Installed on 2025-09-14 17:39:20
# GPU: NVIDIA GeForce RTX 4090 (CUDA accelerated)
# PyTorch installed with CUDA support from https://download.pytorch.org/whl/cu121
# Uncomment these lines if you want to pre-install upscaling support:
# torch==2.4.1
# torchvision==0.19.1
# basicsr>=1.4.2
# realesrgan>=0.3.0
